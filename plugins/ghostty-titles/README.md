# ghostty-titles

Hooks that set the Ghostty tab title to `[icon] [directory]` to show agent activity: `○` session start, `✻` working, `$` shell, `✎` edit, `…` read, `?` needs input, `⌫` compacting, `✓` stopped. No-op outside Ghostty. Requires `jq`.
