{
	"description": "Ensure the full completion pipeline runs before session ends",
	"hooks": {
		"Stop": [
			{
				"hooks": [
					{
						"type": "command",
						"command": "${CLAUDE_PLUGIN_ROOT}/scripts/check-finish-ran.sh",
						"timeout": 5
					}
				]
			}
		]
	}
}
